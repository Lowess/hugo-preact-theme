/** @jsx h */
import { h } from 'preact';

interface Props {

}

const <%= capitalize(name) %> = (props: Props) => {
    return (
        <div>
            Foo! Bar!
        </div>
    );
}

export default <%= capitalize(name) %>
